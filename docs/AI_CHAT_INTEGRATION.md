# AI Chat Integration - Документация

## Обзор

Проект интегрирован с **Timeweb Cloud AI** — сервисом, предоставляющим доступ к модели DeepSeek через OpenAI-совместимый API.

---

## Архитектура

```
┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│   AIChatWidget  │ ────▶ │   /api/chat     │ ────▶ │   Timeweb AI    │
│   (Frontend)    │ ◀──── │   (Next.js API) │ ◀──── │   (DeepSeek)    │
└─────────────────┘       └─────────────────┘       └─────────────────┘
```

### Компоненты

1. **AIChatWidget** (`src/components/AIChatWidget.tsx`)
   - React-компонент
   - UI чата (кнопка, окно, сообщения)
   - Отправляет запросы к `/api/chat`

2. **API Route** (`src/app/api/chat/route.ts`)
   - Next.js API endpoint
   - Проксирует запросы к Timeweb AI
   - Добавляет системный промпт

3. **Timeweb Cloud AI**
   - Внешний сервис
   - Модель: DeepSeek V3.2 (gpt-5-mini)
   - Стоимость: ~60₽/мес за агента

---

## Конфигурация API

### Файл: `src/app/api/chat/route.ts`

```typescript
const TIMEWEB_API_URL = 'https://agent.timeweb.cloud/api/v1/cloud-ai/agents/e7ce10c6-3f8b-4d2a-bd9a-6ad819e6916e/v1/chat/completions';
const TIMEWEB_API_KEY = 'eyJhbGciOiJSUzUxMiIs...'; // JWT токен
```

### Системный промпт

```typescript
const SYSTEM_PROMPT = `Ты — AI-консультант компании Swiss Tech Solutions...

Основные услуги компании:
- Telegram боты и Mini Apps
- Веб-разработка (Next.js, React)
- Мобильные приложения
- AI-интеграции
- Автоматизация бизнес-процессов
- DevOps и облачные решения
- Консалтинг и Discovery Sprint

Правила:
1. Отвечай кратко и по делу (2-4 предложения)
2. Если не знаешь точную цену — предложи связаться для расчёта
3. Будь дружелюбным и профессиональным
4. Отвечай на том языке, на котором пишет клиент
5. При сложных вопросах предлагай оставить контакты

Контакты: форма на странице /contact`;
```

---

## API Формат

### Запрос к `/api/chat`

```http
POST /api/chat
Content-Type: application/json

{
  "messages": [
    { "role": "user", "content": "Какие услуги вы предоставляете?" }
  ]
}
```

### Успешный ответ

```json
{
  "message": "Мы предоставляем: разработку Telegram-ботов и Mini Apps, веб-разработку (Next.js, React), мобильные приложения, AI‑интеграции, автоматизацию бизнес‑процессов, DevOps и облачные решения. Для оценки стоимости оставьте заявку на /contact."
}
```

### Ответ с ошибкой

```json
{
  "message": "Произошла ошибка соединения. Попробуйте позже."
}
```

---

## Виджет чата

### Файл: `src/components/AIChatWidget.tsx`

### Функциональность
- 💬 Плавающая кнопка в правом нижнем углу
- 📱 Адаптивный дизайн
- ⏳ Индикатор загрузки
- 📝 История сообщений (в рамках сессии)
- ❌ Кнопка закрытия

### Состояния

```typescript
interface Message {
  role: 'user' | 'assistant';
  content: string;
}

const [isOpen, setIsOpen] = useState(false);      // Открыт/закрыт
const [messages, setMessages] = useState<Message[]>([]);  // История
const [input, setInput] = useState('');           // Ввод пользователя
const [isLoading, setIsLoading] = useState(false); // Загрузка
```

### Стилизация
- Тёмная тема (void-900, void-950)
- Лазерные акценты (laser-cyan)
- Blur-эффект backdrop
- Framer Motion анимации

---

## Timeweb Cloud AI

### Панель управления
https://timeweb.cloud/my/cloud-ai

### Агент
- **ID:** e7ce10c6-3f8b-4d2a-bd9a-6ad819e6916e
- **Модель:** DeepSeek V3.2
- **Лимит токенов:** 1 000 000/мес (включено)

### Получение API ключа
1. Войти в https://timeweb.cloud
2. Перейти в Cloud AI → API Keys
3. Создать новый ключ
4. Скопировать JWT токен

### Формат URL
```
https://agent.timeweb.cloud/api/v1/cloud-ai/agents/{AGENT_ID}/v1/chat/completions
```

---

## Расширение функциональности

### 1. Обучение агента

Timeweb позволяет загружать файлы для обучения:
- Документы (PDF, DOCX)
- Текстовые файлы
- FAQ в формате JSON

**Рекомендуемый контент для обучения:**
- Описание всех услуг
- Прайс-лист
- FAQ
- Кейсы и примеры работ
- Процесс работы
- Контактная информация

### 2. Улучшение системного промпта

```typescript
const SYSTEM_PROMPT = `
Ты — AI-консультант Swiss Tech Solutions.

## Контекст компании
Swiss Tech Solutions — IT-компания, специализирующаяся на разработке 
для европейского рынка. Офис в Швейцарии, команда распределённая.

## Услуги и примерные цены

### Telegram боты
- Простой бот (уведомления, FAQ): от CHF 2,000
- Средний (интеграции, оплата): от CHF 5,000
- Сложный (AI, CRM): от CHF 10,000+

### Веб-разработка
- Landing page: от CHF 3,000
- Корпоративный сайт: от CHF 8,000
- Веб-приложение: от CHF 15,000+

### Мобильные приложения
- MVP: от CHF 20,000
- Full app: от CHF 50,000+

## Правила ответов
1. Отвечай на языке клиента
2. Краткость: 2-4 предложения
3. Не называй точные цены — только диапазоны
4. Предлагай заполнить форму на /contact для расчёта
5. Если вопрос не о компании — вежливо перенаправь
`;
```

### 3. Интеграция с CRM

```typescript
// После успешного диалога можно отправлять лиды
async function sendLeadToCRM(messages: Message[]) {
  await fetch('/api/crm/lead', {
    method: 'POST',
    body: JSON.stringify({
      source: 'ai_chat',
      conversation: messages,
      timestamp: new Date().toISOString()
    })
  });
}
```

### 4. Аналитика

```typescript
// Отслеживание событий
function trackChatEvent(event: string, data?: object) {
  if (typeof gtag !== 'undefined') {
    gtag('event', event, {
      event_category: 'AI Chat',
      ...data
    });
  }
}

// Использование
trackChatEvent('chat_opened');
trackChatEvent('message_sent', { question_length: input.length });
trackChatEvent('response_received', { response_time: elapsed });
```

---

## Безопасность

### Текущее состояние
⚠️ API ключи хранятся в коде — это временное решение.

### Рекомендации

1. **Переменные окружения**
```bash
# .env.local (не коммитить!)
TIMEWEB_API_URL=https://agent.timeweb.cloud/...
TIMEWEB_API_KEY=eyJhbG...
```

```typescript
// route.ts
const TIMEWEB_API_URL = process.env.TIMEWEB_API_URL!;
const TIMEWEB_API_KEY = process.env.TIMEWEB_API_KEY!;
```

2. **Rate Limiting**
```typescript
// Добавить ограничение запросов
import { rateLimit } from '@/lib/rate-limit';

const limiter = rateLimit({
  interval: 60 * 1000, // 1 минута
  uniqueTokenPerInterval: 500
});

export async function POST(request: NextRequest) {
  try {
    await limiter.check(10, 'CHAT_LIMIT'); // 10 запросов в минуту
  } catch {
    return NextResponse.json({ message: 'Слишком много запросов' }, { status: 429 });
  }
  // ...
}
```

3. **Валидация ввода**
```typescript
// Ограничение длины сообщения
if (input.length > 1000) {
  return NextResponse.json({ message: 'Сообщение слишком длинное' }, { status: 400 });
}
```

---

## Troubleshooting

### Ошибка "API error: 401"
- Проверить актуальность API ключа
- Убедиться, что агент активен в Timeweb

### Ошибка "API error: 429"
- Превышен лимит запросов
- Подождать или увеличить тариф

### Ошибка "Network error"
- Проверить доступность Timeweb API
- Проверить firewall на сервере

### Долгий ответ
- Обычное время: 3-7 секунд
- При высокой нагрузке может быть дольше
- Рассмотреть streaming ответов

---

## Тестирование

### cURL тест
```bash
curl -X POST http://localhost:3000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"messages":[{"role":"user","content":"Привет"}]}'
```

### Production тест
```bash
curl -X POST http://89.19.214.13/api/chat \
  -H "Content-Type: application/json" \
  -d '{"messages":[{"role":"user","content":"Какие услуги?"}]}'
```

---

*Последнее обновление: 3 декабря 2025*
